# Rails Code Review

Review the given changes (or the latest diff) using the **rails-code-reviewer** agent.

If the user specified files or a commit, limit the review to that scope. Otherwise, review the most relevant changed files.
