# Run Format (Biome)

Format TypeScript and JavaScript source files with Biome.

1. Run `npm run format` or `npx biome format --write .` (or project format script) to format all configured files.
2. If the user specified a path, run Biome format on that path only.
3. Ensure no formatting-related changes are left unapplied; report any Biome errors.
