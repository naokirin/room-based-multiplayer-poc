# Run Tests and Fix Failures

Run the project test suite and fix any failures using the **rails-tester** agent.

If the user specified files or directories, run tests for that scope and prioritize fixing failures there.
