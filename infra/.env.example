# Copy to .env and set values. Required for docker compose -f infra/docker-compose.yml up
# See README and specs/002-docker-compose-apps/quickstart.md for procedure.

JWT_SECRET=
INTERNAL_API_KEY=

# Optional: API CORS allowed origins (comma-separated). If unset, api-server uses dev defaults (localhost:5173, etc.).
# CORS_ALLOWED_ORIGINS=
