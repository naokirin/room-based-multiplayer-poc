#!/bin/bash -e

# When running rails server, prepare DB and seed so game_types and test users exist
if [[ "$1" == *rails* ]] && [[ "${2:-}" == "server" ]]; then
  ./bin/rails db:prepare
  ./bin/rails db:seed
fi

exec "${@}"
