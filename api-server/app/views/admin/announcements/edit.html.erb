<div class="page-header">
  <h1>Edit Announcement</h1>
  <%= link_to "Back", admin_announcement_path(@announcement), class: "btn btn-secondary" %>
</div>

<div class="card">
  <%= render "form", announcement: @announcement %>
</div>
